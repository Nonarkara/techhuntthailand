{
  "name": "smart-city-thailand-tech-hunt",
  "version": "1.0.0",
  "description": "Trilingual Smart City Thailand Tech Hunt showcase hub with directory, live pulse, and partner pages.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check:syntax": "node --check server.js && node --check app.js && node --check live-pulse.js && node --check site-pages.js && node --check data.js && node --check scripts/smoke-test.js && node --check scripts/build_pages_bundle.js",
    "check:smoke": "node scripts/smoke-test.js",
    "test": "npm run check:syntax && npm run check:smoke",
    "build:data": "python3 scripts/build_directory_data.py",
    "build:pages": "node scripts/build_pages_bundle.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "google-trends-api": "^4.9.2"
  }
}
